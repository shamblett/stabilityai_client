name: 'stabilityai_client'
version: '1.0.0'
description: 'A server and browser based Stability.ai REST client allowing the generation and manipulation of images from text prompts.'
repository: https://github.com/shamblett/stabilityai_client
homepage: https://github.com/shamblett/stabilityai_client

funding:
  - https://www.darticulate.com/#funding
  
environment:
  sdk: '>=3.0.0 <4.0.0'

  
dependencies:
  async: '^2.11.0'
  http_parser: '^4.0.2'
  intl: '^0.18.1'
  meta: '^1.9.1'
  
dev_dependencies:
  test: '^1.25.0'
  lints: '^3.0.0'
  build_runner: '^2.4.7'
  build_test: '^2.2.2'
  build_web_compilers: '^4.0.7'
